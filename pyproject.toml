[project]
name = "teamswallpaperengine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "typer>=0.27.2",
]

# PyPI is blocked on the corporate network; resolve through the internal proxy feed.
[[tool.uv.index]]
name = "packagefeedproxy"
url = "https://packagefeedproxy.microsoft.io/pypi/simple/"
default = true
